# Optional. If set, the contact form POSTs JSON to this URL.
# Leave unset for the default no-op (client-side success, no network call).
# NEVER put secrets here — this file ships to the browser.
VITE_CONTACT_ENDPOINT=
