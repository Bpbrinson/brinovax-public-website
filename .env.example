# Optional override for the contact-form endpoint.
# The production endpoint is baked into src/data/site.ts, so you normally don't need
# this. Set it to point at a different environment, or to "" to disable the POST and
# fall back to the mailto flow (opens the visitor's mail client).
# NEVER put secrets here — this file ships to the browser.
VITE_CONTACT_ENDPOINT=
